export enum TBankAccount {
  savings = 'savings',
  current = 'current'
}

export enum TWithdrawalRequstType {
  bank = 'bank',
  bkash = 'bkash',
  nagad = 'nagad',
  rocket = 'rocket'
}

export enum TAccountType {
  personal = 'personal',
  merchant = 'merchant'
}